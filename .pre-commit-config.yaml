default_install_hook_types: [pre-commit, commit-msg]
repos:
-   repo: local
    hooks:
      - id: validate-commit-msg
        name: Validate commit format
        entry: .githooks/validate-commit-msg.sh
        language: script
        stages: [commit-msg]